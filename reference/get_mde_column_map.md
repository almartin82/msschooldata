# Get column mapping for MDE data

Returns a mapping of MDE column names to standardized names. MDE column
names vary slightly across years.

## Usage

``` r
get_mde_column_map()
```

## Value

Named list of column mappings
